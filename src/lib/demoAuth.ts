/**
 * HttpOnly session cookie for /demo access after POST /api/demo-auth succeeds.
 * Name is stable so middleware or server checks can import the same constant.
 */
export const DEMO_AUTH_COOKIE = "fit-room_demo_auth";
